buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.12.1'
    }
}

ext {
    compileSdkVersion = 19
    buildToolsVersion = "20"
}

allprojects {
    group = 'eu.inloop'
    version = '0.1.3'
}
